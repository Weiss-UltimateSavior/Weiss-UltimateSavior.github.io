{
  "name": "weiss-ultimatesavior-docs",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vitepress dev",
    "build": "vitepress build",
    "preview": "vitepress preview",
    "docs:dev": "vitepress dev",
    "docs:build": "vitepress build",
    "docs:publish": "npm run docs:build && node scripts/publish-docs.mjs",
    "docs:preview": "vitepress preview"
  },
  "devDependencies": {
    "vitepress": "2.0.0-alpha.15"
  }
}
